{
  "name": "auranet",
  "private": true,
  "version": "0.1.0",
  "description": "Activity logger + aura blending + paper sketch map + tasks (static site).",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "http-server web -p 5173 -c-1",
    "test": "node --test policy/evaluator.test.js"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
