{
  "name": "auranet",
  "private": true,
  "version": "0.1.0",
  "description": "Offline-first task board + focus timer (static site).",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "http-server web -p 5173 -c-1"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}

